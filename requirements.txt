# Steam to TierMaker GUI Application Requirements
# Core dependencies
selenium>=4.15.0
requests>=2.31.0
Pillow>=10.0.0

# GUI dependencies (tkinter is included with Python)
# No additional GUI packages needed - using built-in tkinter
